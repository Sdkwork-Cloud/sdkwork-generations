# Application Plugins

Application-local plugins may be added here when `sdkwork-generations-pc` needs local agent extensions.

Plugins must be documented, non-secret, and scoped to this application root.
