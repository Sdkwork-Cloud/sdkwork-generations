# Application Skills

Application-local skills may be added here when `sdkwork-generations-pc` needs workflows that narrow repository or root SDKWork standards.

Skills must cite canonical specs under `../../../sdkwork-specs/` and must not store source code, generated SDK output, runtime data, or secrets.
