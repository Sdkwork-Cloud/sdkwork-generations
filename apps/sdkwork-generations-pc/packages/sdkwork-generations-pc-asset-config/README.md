# @sdkwork/generations-pc-asset-config

Single source of truth for generation asset configuration, credit estimation, and model bucket resolution used by `@sdkwork/generations-pc-workspace` and domain `@sdkwork/*-pc-generation` packages.

Domain packages re-export this module; do not fork implementation copies locally.
