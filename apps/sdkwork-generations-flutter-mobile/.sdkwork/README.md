# SDKWork Generations Flutter Mobile Workspace

This `.sdkwork/` directory stores source-controlled workspace metadata for the `sdkwork-generations-flutter-mobile` application root.

It is not runtime state, generated SDK output, or user-private configuration. Local-only data belongs under ignored `local/`, `tmp/`, `cache/`, or `secrets/` paths.