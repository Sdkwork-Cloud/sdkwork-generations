# SDKWork Generations Flutter Mobile Plugins

This directory stores repository/application-local agent extensions and plugin bundles for the SDKWork Generations Flutter mobile application.

Plugins follow `SDKWORK_WORKSPACE_SPEC.md` and must not vendor unrelated external toolchains.