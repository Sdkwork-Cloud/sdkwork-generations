# SDKWork Generations H5 Plugins

This directory stores repository/application-local agent extensions and plugin bundles for the SDKWork Generations H5 application.

Plugins follow `SDKWORK_WORKSPACE_SPEC.md` and must not vendor unrelated external toolchains.