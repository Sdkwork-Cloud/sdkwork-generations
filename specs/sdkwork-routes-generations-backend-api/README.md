# sdkwork-routes-generations-backend-api

Backend-facing HTTP API for the generations module. Exposes service-to-system endpoints under `/backend/v3/api` for receiving source events, job results, and administrative operations.