# sdkwork-routes-generations-app-api

Application-facing HTTP API for the generations module. Exposes authenticated end-user endpoints under `/app/v3/api` for creating and managing AI generation requests.