# sdkwork-api-generations-assembly

Runtime assembly crate that composes the generations database host, service layer, and HTTP axum routes into a gateway-ready API contribution. Serves as the integration entrypoint for the platform cloud gateway.
