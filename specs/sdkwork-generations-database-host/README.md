# sdkwork-generations-database-host

Database host crate for the generations module. Owns PostgreSQL connection pooling, schema lifecycle, repository implementations, and query helpers for all `generation_*` tables.